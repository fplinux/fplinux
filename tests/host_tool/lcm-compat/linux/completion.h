/* SPDX-License-Identifier: GPL-2.0-only */
#ifndef LCM_HOST_COMPLETION_H
#define LCM_HOST_COMPLETION_H
/* Layout placeholder only: the transport never operates on completions. */
struct completion {
	unsigned int unused;
};
#endif
